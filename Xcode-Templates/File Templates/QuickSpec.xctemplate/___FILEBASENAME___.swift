@testable import client_ios
import Nimble
import Quick

class ___FILEBASENAMEASIDENTIFIER___: QuickSpec {
    override func spec() {
        describe("something") {
            context("when this") {
                it("does that") {
                    expect(hello).to(equal(world))
                }
            }
        }
    }
}
